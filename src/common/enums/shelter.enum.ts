export enum ShelterStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum ShelterRole {
  OWNER = 'owner',
  DIRECTIVE = 'director',
  MEMBER = 'publicator',
}
